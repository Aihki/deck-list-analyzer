import os
import requests
from dotenv import load_dotenv

load_dotenv()

scryfall_api_url = os.getenv('scryfall-api')

def search_card(card_name):
    return

def get_card_colors(card_name):
    return